# analysis

Pipelines and scripts for downstream analysis.

Planned:
- `blast_barcode.py` — submit FASTA to BOLD and NCBI, parse top hits, apply species-call thresholds, push results to LibreBiotech Sample records
- `trim_sanger.py` — quality-trim Sanger reads and build consensus

Currently empty — contributions welcome.
